#!/bin/groovy
node (params.nodeName){
def cfg = [:]
try {
stage('Terraform Init') {
checkout scm
tfInit(cfg)
}
stage('Terraform Do') {
            // Perform Terraform plan/apply/destroy
            tfDo(cfg)
        }
        

}

}
