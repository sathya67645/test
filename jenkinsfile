#!/bin/groovy
node (params.nodeName){
def cfg = [:]

stage('Terraform Init') {
checkout scm
tfInit(cfg)
}
stage('Terraform Do') {
            // Perform Terraform plan/apply/destroy
            tfDo(cfg)
        }
        



}
